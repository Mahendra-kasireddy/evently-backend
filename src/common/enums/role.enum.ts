/**
 * Application roles. A user may hold several (e.g. an organizer who is also a customer).
 */
export enum Role {
  CUSTOMER = 'customer',
  ORGANIZER = 'organizer',
  VENDOR = 'vendor',
  ADMIN = 'admin',
}
